apiVersion: v1
kind: Service
metadata:
  name: weather-api
  namespace: weather-mlops
  labels:
    app.kubernetes.io/name: weather-api
    app.kubernetes.io/part-of: weather-mlops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: weather-api
  ports:
    - name: http
      port: 8000
      targetPort: http
